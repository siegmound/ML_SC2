# Block 16

This block formalizes the project end-state using the reproducible full multi-seed results already produced in the remake.

Scope:
- collect full multi-seed RF and XGB results for `v3_1_fixed` and `v3_2_combatfix`
- generate a final summary table
- write a freeze-style final report
- verify that the key runner scripts and result zips are present

Key conclusion:
- RF (`no_counter`) and XGB are effectively tied on full multi-seed evaluation.
- `v3_1_fixed` is at least as strong as `v3_2_combatfix` at final scale.
