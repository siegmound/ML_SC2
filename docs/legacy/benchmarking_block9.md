# Block 9 Benchmarking

Block 9 promotes the remake from single-seed smoke runs to staged multi-seed benchmarks on `real_v3_1_fixed_smoke1500`.

Scope:
- repeated replay-group-aware splits for seeds 42, 43, 44
- consistent evaluation for `logreg`, `rf`, and `xgb`
- aggregation of mean and standard deviation across seeds

These are candidate-final staged benchmarks, not yet full-dataset final claims.
